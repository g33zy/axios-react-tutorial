// whiteboarding for filtering beers by input field


// input from user = searchTerm
// store the data state ={ searchTerm }
// => move data onChange => handleChange
// 
// create a filtered list component
// create an unfiltered list component

// create a list component
// create a filter function that uses the term from state and compares it to the array of beer 

// logic gate aka if else/ternary 
// !this.state.searchTerm ? <unfiltered /> : <filtered />

// filter the data
// method to pass to .filter